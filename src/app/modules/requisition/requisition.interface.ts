export interface IRequisition {
  name: string;
  id?: number;
  faculty: string;
  dept: string;
  ls?: string;
  date: string;
  reason: string;
  isVerified?: string;
  employeeId?: string;
  role?: string;
}
